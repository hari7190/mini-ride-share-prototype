replicaCount: 1

image:
  repository: public.ecr.aws/bitnami/kafka
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 9092

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi
replicaCount: 1
