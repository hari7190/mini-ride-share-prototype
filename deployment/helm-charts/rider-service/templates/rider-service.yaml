apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rider-ingress
  annotations:
spec:
  ingressClassName: nginx 
  rules:
  - http:
      paths:
      - path: /api/rider
        pathType: Prefix
        backend:
          service:
            name: rider-service
            port:
              number: 8081
---
apiVersion: v1
kind: Service
metadata:
  name: rider-service
  namespace: dev
spec:
  selector:
    app: rider-service  
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-rider-service
spec:
  revisionHistoryLimit: 3
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: rider-service
  template:
    metadata:
      labels:
        app: rider-service
    spec:
      imagePullSecrets:
        {{- toYaml .Values.imagePullSecrets | nindent 8 }}
      containers:
        - name: rider-service
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: 8080
          env:
            - name: MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mysql-pass-secret 
                  key: password
            - name: MYSQL_USERNAME
              valueFrom:
                secretKeyRef:
                  name: mysql-user-name
                  key: username
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: jwt-secret
                  key: secret
            - name: JAVA_OPTS
              value: "-Xms128m -Xmx256m -XX:MaxMetaspaceSize=128m" # optimzing memory usage
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:mysql://{{  .Values.env.MYSQL_HOST}}:{{ .Values.env.MYSQL_PORT }}/{{ .Values.env.MYSQL_DATABASE }}"
            - name: KAFKA_BOOTSTRAP_SERVERS
              value: {{ .Values.kafka.bootstrapServers }} # TODO: make this dynamic
