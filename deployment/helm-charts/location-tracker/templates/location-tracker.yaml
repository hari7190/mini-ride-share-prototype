apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: location-tracker-ingress
  annotations:
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - path: /api/locationtracker
        pathType: Prefix
        backend:
          service:
            name: location-tracker
            port:
              number: 8084
---
apiVersion: v1
kind: Service
metadata:
  name: location-tracker
  namespace: dev
spec:
  selector:
    app: location-tracker
  ports:
    - protocol: TCP
      port: 8084
      targetPort: 8084
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-location-tracker
spec:
  revisionHistoryLimit: 3
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: location-tracker
  template:
    metadata:
      labels:
        app: location-tracker
    spec:
      imagePullSecrets:
        {{- toYaml .Values.imagePullSecrets | nindent 8 }}
      containers:
        - name: location-tracker
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: 8084
          env:
            - name: MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mysql-pass-secret
                  key: password
            - name: MYSQL_USERNAME
              valueFrom:
                secretKeyRef:
                  name: mysql-user-name
                  key: username
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: jwt-secret
                  key: secret
            - name: JAVA_OPTS
              value: "-Xms128m -Xmx256m -XX:MaxMetaspaceSize=128m" # optimzing memory usage
            - name: MYSQL_HOST
              value: "{{ .Values.env.MYSQL_HOST }}"
            - name: KAFKA_BOOTSTRAP_SERVERS
              value: {{ .Values.kafka.bootstrapServers }} # TODO: make this dynamic
